### Step 13 — Validate CHECKER-TOKEN
- Tool Used:
  ```
  mcp restapi-mcp-server -> listSpecificEnvironmentVariable
  ```
- Validation:
  - Confirm `CHECKER-TOKEN` exists and is populated.
- Success Condition:
  - Checker token available for authenticated calls.

