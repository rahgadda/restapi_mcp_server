### Step 11 — Validate MAKER-TOKEN
- Tool Used:
  ```
  mcp restapi-mcp-server -> listSpecificEnvironmentVariable
  ```
- Validation:
  - Confirm `MAKER-TOKEN` exists and is populated.
- Success Condition:
  - Maker token available for authenticated calls.

